# Synthetic fixtures

These fixtures are safe demo data for CI and UI smoke only. They are not derived from a patient record and must never be replaced with an export from HOSxP.

- `ready`: discharged case with documented evidence and a valid illustrative grouper result
- `attention`: admitted case with missing PDx and an explicit empty grouper response
